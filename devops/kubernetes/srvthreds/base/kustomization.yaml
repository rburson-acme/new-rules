apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Note: namespace, service-account, and secret-provider-class are managed by Terraform
# in the AKS stack to ensure proper value injection from infrastructure

resources:
  - configmap-managed-services.yaml
  - rabbitmq-statefulset.yaml
  - rabbitmq-service.yaml
  - srvthreds-engine.yaml
  - srvthreds-session-agent.yaml
  - srvthreds-persistence-agent.yaml
  - srvthreds-bootstrap-job.yaml
  - srvthreds-ingress.yaml
