apiVersion: apps/v1
kind: Deployment
metadata:
  name: srvthreds-session-agent
  namespace: srvthreds
  labels:
    app.kubernetes.io/name: srvthreds-session-agent
    app.kubernetes.io/component: session-agent
    app.kubernetes.io/part-of: srvthreds
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: srvthreds-session-agent
      app.kubernetes.io/component: session-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: srvthreds-session-agent
        app.kubernetes.io/component: session-agent
    spec:
      containers:
      - name: session-agent
        image: srvthreds:latest
        command: ["node", "/app/srvthreds/dist-server/agent/agent.js", "-c", "session_agent", "-i", "session-agent", "-d"]
        envFrom:
        - configMapRef:
            name: srvthreds-config
        ports:
        - containerPort: 3000
          name: http
        - containerPort: 3001
          name: websocket
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: session-agent
  namespace: srvthreds
  labels:
    app.kubernetes.io/name: srvthreds-session-agent
    app.kubernetes.io/component: session-agent
    app.kubernetes.io/part-of: srvthreds
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  - name: websocket
    port: 3001
    targetPort: 3001
  selector:
    app.kubernetes.io/name: srvthreds-session-agent
    app.kubernetes.io/component: session-agent